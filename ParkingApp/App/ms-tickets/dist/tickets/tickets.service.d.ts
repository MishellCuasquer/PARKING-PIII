import { Repository } from 'typeorm';
import { CreateTicketDto } from './dto/create-ticket.dto';
import { UpdateTicketDto } from './dto/update-ticket.dto';
import { Ticket } from './entities/ticket.entity';
import { ConfigService } from '@nestjs/config';
import { HttpClientService } from "../common/htppl-cliente.service";
import { ServiceTokenService } from '../auth/service-token.service';
export declare class TicketsService {
    private readonly ticketRepository;
    private readonly httpClient;
    private readonly configService;
    private readonly serviceTokenService;
    private readonly logger;
    private readonly personaUrl;
    private readonly espacioUrl;
    private readonly vehiculosUrl;
    private readonly tarifaPorHora;
    constructor(ticketRepository: Repository<Ticket>, httpClient: HttpClientService, configService: ConfigService, serviceTokenService: ServiceTokenService);
    create(createTicketDto: CreateTicketDto, authorization?: string): Promise<Ticket>;
    findAll(): Promise<Ticket[]>;
    findOne(id: string): Promise<Ticket>;
    findActivos(): Promise<Ticket[]>;
    cerrarTicket(id: string, updateTicketDto: UpdateTicketDto, authorization?: string): Promise<Ticket>;
    remove(id: string): Promise<void>;
    private validarPersona;
    private validarPlaca;
    private buscarEspacioDisponible;
    private validarTicketActivo;
    private toDate;
    private calcularHorasCobro;
    private calcularCosto;
    private emitirTicket;
    private actualizarEstadoEspacio;
}
